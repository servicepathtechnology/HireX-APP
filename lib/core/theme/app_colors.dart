import 'package:flutter/material.dart';

/// HireX design system color tokens.
/// Always reference these — never use raw hex values in UI code.
abstract class AppColors {
  static const primary = Color(0xFFE94560);
  static const background = Color(0xFF1A1A2E);
  static const surface = Color(0xFF16213E);
  static const surfaceVariant = Color(0xFF0F3460);
  static const onPrimary = Color(0xFFFFFFFF);
  static const onBackground = Color(0xFFFFFFFF);
  static const onSurface = Color(0xFFE0E0E0);
  static const error = Color(0xFFCF6679);
  static const success = Color(0xFF4CAF50);
  static const warning = Color(0xFFFF9800);
  static const divider = Color(0xFF2A2A4A);
  static const shimmer = Color(0xFF2E2E50);
}
